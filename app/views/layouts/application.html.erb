<!DOCTYPE html>
<html>
  <head>
    <title>Collabrador</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div data-controller="modals"> <!-- Note: modals-controller sits in seperate div to cover the navbar and the modals itself -->
      <div class="navbar navbar-light bg-light justify-content-between sticky-top m-2">
        <div class="mr-auto">
          <h3 class="navbar-brand mr-auto">Collabrador</h3>
        </div>
        <div class="">
          <ul class="list-text">
            <li class="btn btn-primary list-item">Login</li>
            <li class="btn btn-light list-item"  data-action="click->modals#openSignup">Signup</li>
          </ul>
        </div>
      </div>


      <!-- Modal -->
      <div class="modal-custom" data-modals-target="signup">
        <div class="inner">
          <div class="modal-head">
            <h1>Sign-up</h1>
            <span><i class="fa-solid fa-circle-xmark" data-action="click->modals#closeSignup"></i></span>
          </div>
          <h2>Join other collabradors in your neighborhood!</h2>
          <%= render "devise/registrations/signup_form" %>
        </div>
      </div>

    <%= yield %>
  </body>
</html>
